```
// formdata
    const formData = new FormData();
    formData.append("code", appUserCode);
    formData.append("wallet", appName);
    // const formDataStr = 'code=' + appUserCode + '&wallet=' + appName;

    // 参数形式
    //const _body = `code=${appUserCode}&wallet=${appName}`;

    // json  JSON.stringify(data)
    // const jsonData = {
    //     code: appUserCode,
    //     wallet: appName
    //   };
    var t = {
        method: 'POST',
        headers: {
            //'Content-Type':'application/x-www-form-urlencoded' // ios or other deal.
            'content-type': 'multipart/form-data'       // android special deal method.
        },
        body: formData
    }, n = globalPostUrl, result;

    console.log("AAA-begin bind user:" + encodeCiyu);
    await fetch(n, t).then(e => {
        return e.text();
    }).then(e => {
        result = e;
    }).then(e => {})
    .catch((error) => {
        console.error("AAA-报错: " + error);
      });
    console.log("AAA-goood:" + result);
};
```

安卓的请求数据需要添加：'content-type': 'multipart/form-data'       // android special deal method.