今日结果：
      1  菜单：  base class QmainWindow ;     
                创建菜单：  一级菜单 （QMenu) 只要有下一级就是一级菜单
                        QMenu \*menuEdit = new Qmenu("Edit");
                二级菜单：QAction \*actioncut = new QAction("QAct");
      2  例子：   QMenuBar \* mneuBar = new QMenuBar;  //  菜单栏
                       QMenu \*menuFile = new Qmenu("File");  // 一级菜单
                       menuBar->addmenu(menuFile);
                       QAction \*actNew = menuFile->addAction("New");  //  二级菜单
                       QAction \*actOpen = menuFile->addAction("Open"); 
      3  Qmenu \*menuopen = menuFile->addMenu("Open"); / /  将open设置为一级菜单放在file子菜单中
            工具栏：  QToolBar \*toolBar = new QToolBar;  // 创建菜单栏
                            toolBar->addaction(QIcon(tr("MSN.ico")), tr("&File"));   // 为工具栏添加图标  ||并设置快捷键(ctrl +N)    menFile->setshortcut("ctrl  + N");     ||  Qmenu \*menu = new Qmenu("&File"); // 快捷键F
            状态栏： QstaticBar \*statusBar = new QstatusBar ;
            工作区： Qworkspace \*workspace = new Qworkspace(); // 创建工作区
                           this->setcentralWidget(workspace);
明日计划：掌握qt中制作菜单栏的过程，用qt 实现对sqlite数据库的增删改查。下来就要完成学校这边的作业，熟悉linux系统命令行操作。


感想：觉得QT是一个很强大的工具，里面有好多的方法，接口，属性等等，如果让自己去记这些东西的话，感觉对自己很难，但可以像认识单词一样不必记住，只需要在见到它是知道他是什么意思。然而要知道这么多的方法，借口，属性的意思，那就要自己就经常性的去熟悉它们的使用和意思。总之，要多使用，多发现，多琢磨为什么。