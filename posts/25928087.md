今日结果：
     1 函数的两种使用方法，一种声明，后定义；另一种是直接先定义后就可以在main（）中使用了。
     2 max(int a , int b);中a ，b 是形式参数；max( 2 , 3 ) 2，3分别是实参。
     3 定义常量的方法：#define PI 3.14159 ;  const  int   a= 9;   enum fuc{ a , b = 5, c , d =8}。
     4    return z  等价于  return (z)。
     5  对于主函数 main() 前面的默认值为   void 的，其他自定义函数默认为  int 。
     6   多个函数的嵌套调用，和函数递归调用（自己调用自己）。
                        int   func( int n)
                            {
                                 if(n<=1)
                                       return   1;
                                  else
                                         return  ( n\* fuc(n-1));
                             }
        7 一般的变量都是默认为 auto 的，static int a;  只在开始使用一次，以后的函数调用都不必重新赋值了；
             extern 进行另一个.c 文件的引用，属于外部变量引用；register int  a; 加快变量的执行速度，因为寄存器的速度远快于               内存。
        8  函数调用是 传值不会改变原始的值，但传地址是会改变原始值。
        
明日计划：
         多练习函数的编程，自己不断的动手编程，如果有时间就会C语言书进行预习。
感想：
       我发现自己也快成为电脑脸了，坐在电脑面前，自己都快变成电脑了。偶尔听一听歌曲，好像整个人都醉了一样。好希望自己快点跑，希望能留给更多的时间小娱乐一下哈。。。。。