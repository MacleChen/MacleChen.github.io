今日结果：
1  file \*  ./   //  查询当前目录下的文件格式
2  服务器，多用户通信的实现：  登录包： 消息类型，发送人， 发送信息
在封装socket的类时，应该要客户端，服务器的函数都统一的封装到一个主类当中。并且合理的提示出错信息
将信息包打包成结构体：包括通信人，通信类型，通信内容
3  在select 的函数中为read， write 设置超时时间，对于登录请求连接时，不必设置超时时间，因为系统可能会自动处理或不会影响服务器的性能。
        4  而主要时间是讲了socket如何细致的封装成类，包括错误提示，参数的传递，那些函数可以封装成成员函数，在设置一个主的处理函数等。


明日计划：完成多用户登录聊天功能的实现，复习以前的学过的难点，常用的知识点，之后对线程进行预习。


感想：感觉有段时间后，自己的心会很乱，什么事情都不想干，有时候就突然很想看一些电视剧，但看这个最大的缺点就是很浪费时间，自己必须在短时间内看完。其实自己已经早有了时间的安排，但是没有按照上面的做。总是被一些微不足道的事情所干扰，这也充分的体现出自己控制力很差，抗干扰能力很差。希望自己说到做到。