这些天公司让做支付宝功能，不的不说小的也是第一次搞这个东西。所以遇到了不少坑，求助了多方大神，还有前辈们在网上留下的技术博客给解决了。为了防止世界被破坏，我决定给iOS新手的宝宝们做一个详细的参考。由于我也是第一次写简书，技术还不成熟，有什么不对的地方请各位看官老爷们留下宝贵的意见。小的在这里谢过了。

---

接入支付宝的大致流程如下：

1.向支付宝申请, 与支付宝签约，获得商户ID（partner）和账号ID（seller）和私钥(privateKey)

（1）生成公钥和秘钥的方法  打开终端

生成私钥pem,执行命令   openssl genrsa -outrsa\_private\_key.pem 1024

![](https://i-blog.csdnimg.cn/blog_migrate/8dd7fe26a50f5f43c54c08f37c1d2955.webp?x-image-process=image/format,png)

生成公钥,执行命令    openssl rsa -in rsa\_private\_key.pem -pubout -out rsa\_public\_key.pem

![](https://i-blog.csdnimg.cn/blog_migrate/04324d47d50930b97fd09528e9ea6d92.webp?x-image-process=image/format,png)

//如果服务器端已经生成公私密钥，那么获取他们的公私密钥文件，然后编码，然后将私钥发给服务器，然后开始调试。

将RSA私钥转换成PKCS8格式,命令执行openssl pkcs8 -topk8 -inform PEM -in

rsa\_private\_key.pem -outform PEM -nocrypt  PHP服务端语言读取私钥不需要PKCS8转换。

2. 下载支付宝的demo

3. 生成订单，签名加密

4.调用支付宝客户端，由支付宝客户端跟支付宝安全服务器打交道

5.支付完毕后,支付宝客户端会自动跳回到原来的应用程序

6.在原来的应用程序中显示支付结果给用户看

---

下面开始正式的接入支付宝，假设你已经获得了 获得商户ID（partner- 一般以2088开头）和账号ID（seller）和私钥(privateKey)

1.先将支付宝demo 中把下图的文件拷贝到工程中

![](https://i-blog.csdnimg.cn/blog_migrate/f187c1a6b176204e3a6419ccfff0a4c8.webp?x-image-process=image/format,png)

这个步骤值得注意的是，不要直接拖拽文件拷贝的工程，这样你的路径会出错。

解决方法先拷贝到一个文件夹中，然后在拷贝到工程中。

2. 添加依赖库，在Build Phases选项卡的Link Binary With Libraries中

![](https://i-blog.csdnimg.cn/blog_migrate/507fe96117ade8a5e62ee28a14592e32.webp?x-image-process=image/format,png)

值得注意的是

如果是Xcode 7.0之后的版本，需要添加libc++.tbd、libz.tbd；

如果是Xcode 7.0之前的版本，需要添加libc++.dylib、libz.dylib

---

可能遇到的困难

1.

![](https://i-blog.csdnimg.cn/blog_migrate/1d9ef8192553d6bd20b33d41e64d89e2.webp?x-image-process=image/format,png)

不要慌张看到这么多错误，这是阿里比较坑的地方，原因是缺少

#import<UIKit/UIKit.h>  和 #import<Foundation/Foundation.h> 这两个头文件在

支付宝中AliSDKDemo-Prefix.pch 这个文件里添加了头文件，我们只需要添加上即可。

---

问题2  找不不这个文件

![](https://i-blog.csdnimg.cn/blog_migrate/ab7745d7b899c6c7bbcea9271f33751f.webp?x-image-process=image/format,png)

解决：Build Settings --> Search Paths --> Header Search paths : $(SRCROOT)/支付宝的文件路径

例如：

![](https://i-blog.csdnimg.cn/blog_migrate/64100258bdf347c5498be5c1076e8755.webp?x-image-process=image/format,png)

![](https://i-blog.csdnimg.cn/blog_migrate/e96ffda5803e671265080956dd2b52fc.webp?x-image-process=image/format,png)

问题3

![](https://i-blog.csdnimg.cn/blog_migrate/792574cd1e3d31ac34f89eaa9dcec537.webp?x-image-process=image/format,png)

遇到这样的问题是因为自己粗心而导致，没有添加下图的这两个文件

![](https://i-blog.csdnimg.cn/blog_migrate/bcc9c88ab3cf328b4ff84a5b5197b278.webp?x-image-process=image/format,png)



---

接下来我们开始生成订单 （必须按照要求严格配置订单，必须严格，必须严格，重要的事情说三遍）

> //1.添加写partner/seller/privateKey,签约后获得
>
> NSString \*partner =@"partner";
>
> NSString\*seller =@"seller";
>
> NSString\*privateKey =@"privateKey";
>
> 2.1.创建订单Order \*order =[[Order alloc] init];
>
> order.partner = partner;
>
> order.sellerID = seller;
>
> order.outTradeNO = [self generateTradeNO]; //订单ID（由商家自行制定）
>
> order.subject = @“”; //商品标题
>
> order.body = @“”; //商品描述
>
> order.totalFee = [NSString stringWithFormat:@"0.01"]; //商品价格
>
> order.notifyURL =  @""; //回调URL
>
> order.service = @"mobile.securitypay.pay";
>
> order.paymentType = @"1";
>
> order.inputCharset = @"utf-8";
>
> order.itBPay = @"30m";
>
> order.showURL = @"m.alipay.com";
>
> //应用注册scheme,在AlixPayDemo-Info.plist定义URL types    
>
> NSString \*appScheme = @"alisdkdemo";        
>
> //将商品信息拼接成字符串    NSString \*orderSpec = [order description];    NSLog(@"orderSpec = %@",orderSpec);       
>
> //获取私钥并将商户信息签名,外部商户可以根据情况存放私钥和签名,只需要遵循RSA签名规范,并将签名字符串base64编码和UrlEncode
>
> idsigner = CreateRSADataSigner(privateKey)；
>
> NSString \*signedString = [signer signString:orderSpec];
>
> //将签名成功字符串格式化为订单字符串,请严格按照该格式
>
> NSString \*orderString = nil;
>
> if (signedString != nil) {
>
> orderString = [NSString stringWithFormat:@"%@&sign=\"%@\"&sign\_type=\"%@\"",orderSpec, signedString, @"RSA"];
>
> [[AlipaySDK defaultService] payOrder:orderString fromScheme:appScheme callback:^(NSDictionary \*resultDic) {
>
> NSLog(@"reslut = %@",resultDic);
>
> }];
>
> }

上面为 生成订单和 签名的代码。 

值得注意的是官方推荐，一般生成订单和签名，获得商户ID（partner- 一般以2088开头）和账号ID（seller）和私钥(privateKey) 都应该放在后台进行，这样极大方面的保护了金钱安全。我们只需要负责调用支付宝的回调方法，来处理结果。

---

配置完成后，我们快点兴奋的来检测一下，诶，好像遇到什么问题了。

我擦嘞，好不容易配置好了，可以测试了，他竟然又出现了错误

#### 显示系统繁忙，请稍后再试（ALI10）

这个问题很好解释，字面意思就是错了呗。 解决方法

1. 你配置的秘钥有问题，由于调用支付宝接口的那个平台，他的秘钥可能过期导致，或者是一些其他的协议出现了问题。

2.请联系客服（不过我没有试过）

---

解决了上面的问题我们在来测试下，oh 买噶的。 又遇到了问题，我也是醉了。

## 交易订单处理失败，请稍后再试。(ALI59)

小伙子们不要慌张，这个问题也是我们粗心而导致的，是因为我们生成的订单有问题。解决方案：

1. 认真核对配置订单的信息，认真核对，认真核对，认真核对。一定要认真，也许是我们订单商品名称错了，订单描述错了，拼接的价钱错了，要记住他们都是 NSString 类型。

---

以上就是本次的内容，不的不说的是支付宝太任性了，我也是爬过了都少个坑才总结的经验。以上的内容也并非我自己解决的，查看了很多技术博客然后自己归纳整编到一起的，我是这方面的菜鸟，我的格言就是——站在巨人的肩膀上编程。谢谢前辈们开创的经验道路，让我们后辈继承。