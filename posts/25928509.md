今日结果：
     1  将函数作为参数传入另一个函数中： void  myInit(struct staff\_t \*p){};     
                                                       void myfunc(struct staff\_t  \*p, void (\*myInit)(struct staff\_t \*).......){};
                                                                               结构体指针参数                函数参数
     2  对于 struct staff\_t   s;   memset(&s,0,sizeof(s));  其中sizeof(s) 与sizeof(struct staff\_t) 效果相同。
     3  int \*p;   p=(int\*)malloc(10\*sizeof(int));分配空间不初始化，使用memset();
                     p=(int\*)calloc(10,2) // calloc(申请单元个数，每个单元的字节数）；申请空间，并且初始化为零；
          strcpy(p,"ifojwofjow"); 赋值之后，  realloc(p,20); 另外重新申请 20个字节的空间，单元的数据都在； 
                realloc(p,0); 和 free(p);  都是释放空间，之后让指针指向空，p=NULL.
     4  main() 函数最多可以带两个参数， main(int argc, char \*argv[])
     5  对于链表，除首尾结点外，都有直接前驱和直接后续。  尾结点的指针指向NULL; 一种有数据域和指针域，另一种有
               指针域，数据域和指针域。
    6  清除缓存函数： fflush(stdin).


明日计划：等会先把双链表的作业完成掉，明天去培训稍微找一点，进行一个链表的预习和复习，之后好好打一场连球赛。


感想：我又脑子发烧了，今天中午没有午休，而且看球赛，车赛，泳赛。虽然看刺激了，但晚上上课有点头晕，真的严格要求自己，让自己好好的在编程上面拼一把，虽然不能做最好的，但希望能做一名很努力的程序员。come on ！！！