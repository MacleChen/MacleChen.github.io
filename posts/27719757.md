今日结果：（Qt的环境搭建和基础按钮的编写）
      1  Qtcreator 一次编写到处编译（代码不变动）； Java 一次编译到处运行； Qt 大部分是C++的代码（属于单根继承）。
      2  Qt软件的具体操作：  构造函数  test ：：test(){    
                                                                                    QpushButton \*btnok = new QpushButton("ok", this)  
                                                                                     // 创建一个按钮                                        名称      显示在当前窗口     
                                                                                    btnok->move(100,50)  //  按钮位置（x, y)
                                                                                   this->resize(800, 600) //  窗口位置
                                                                                   }
      3  在主函数（main中）：  （打印信息）  qDebug()<<"aaa";  ||  qDebug("aaa");  ||qDebug("id = %d\n", 4);   || cout<<"aaaa"<<endl;（要加头文件，兼容性不是很好）。
               字符串： QString s;  //定义    s= "aaaa"; // 赋值    qDebug()<<"s = "<<s;  //  打印  （qDebug() 会自动换行）   
      4  转型： int  aa;   QString  qs;    aa = qs.toInt();     
                     string s;  qs = "aaaa";  s  qs.tostdstring();  //  从QSstring  转为 string；
                             s  =  (char \*) ( qs.tostdstring().c\_str());//   先转化为标准字符串，再转化为字符
      5  类模板： QList <int> listA;    listA.append(1) ;   listA.append(5);   //  下标自动从零开始，为其赋值
                           for( int i = 0; i < listA.count(); i++)
                                        qDebug()<<"lisA["<<i<<"]="<<listA.at(i);    //   listA.at(i)  代表第i个值
                        listA.remoreAt(1);  // 删除第一个结点         listA.replace(0, 10);  // 修改第0个结点为10.
                       t.setwindowTile("man manager");  //  改变窗口名称  （显示中文要另加代码）
                       t.setWindowIcon((QIcon),"D:/msnms.ico"); //  修改图标（ico）
                       return   app.exec();   //  搜索事件（获取操作事件）
      6     QLineEdit \*Edtid = new  QLineEdit;   // 输入框 （名称为Edtid)
             Edtid->setparent(this)  ;   // 将控件放在当前窗口上
             QLabel \*lblid = new QLabel(this) ;   //  标签的定义    lblid->move(400, 50);  //    标签放在窗口的位置（大约5个像素点代表一个字符空格）
       7  QDialog 类：  模式： 弹出一个新的窗口是，前面的窗口不能被点击；  非模式：弹出一个新的窗口，和前面的窗口互换点击。
            切换窗口时： MAN:类中  有   Q\_OBJECT     private slots(槽）：void slokok(); // 点击时间相应函数
                                             void  text:slotok(){    MAN  m;    m.show()  ||  m.exec() }  前一个相对位置方式显示，后一个绝                                                                                                                                      
                                对位置方式显示
                                            connect(btnid,  SINGAL(clicked()), this , SLOT(slotok));   //  连接另一个窗口
             QMessageBox::critical(0, "title", "this is a test ", QMessageBox::Ok,...);       //  有返回值
                                                    名称       内容                  显示按钮          可继续增加按钮    
             QMessageBox::warning(...)  ; // 警告按钮提示    QMessageBox::information(...);  //信息按钮提示                                              QMessageBox::about(...);  // 关于按钮提示
       8  Qt  的布局管理器：  绝对位置方式：通过QWidge， 控件的位置固定，改变其窗口，控件不会做出调整 
                                           相对位置方式： 改变其窗口，控件会做出相应的调整
                                           edtpwd->setGeometry(10,  5 , 50 ,20); //  x(水平）， y(竖直），长， 宽
                               QHBoxLayout  \*hlayid = new QHBoxLayout;  //   定义水平布局管理器
                                      hlayid->addwidget(lblid);     hlayid->addwidget(edtpwd);  //  加入所要的水平控件
                              QVBoxLayout \*laymain = new QVBoxLayout;  // 定义竖直布局管理器
                                      laymain->addLayout(Hlayid); // 将水平管理器放置在竖直的管理器中
                              this->setLayout(laymain);  //将竖直布局管理器放置到当前窗口上
                                      btnok->setMinimumWidth(200) ;  //  设置按钮的宽度
                    信号/ 槽： 对硬件设备的操作做出相应
                               QTextEdit  \*txtMemo = new QTextEdit;  //  文本输入框
                                                           
明日计划： 搭建Qt的环境，连接sqlite数据库, 完成Qt相应的作业，  熟悉Qt环境下的编程
感想：感觉第二阶段快要接近尾声了，一直都感觉Qt挺神奇的，听同学说Qt和MFC差不多，但自己感觉Qt,更高级，更时尚，界面看起来更美观。希望自己也能尽量多的了解Qt。自己现在的状态真的好像又进入到一个’高三‘ 的生活，很从实，有方向感。对自己来说可能最需要这种状态。Everyone  is  number one~~~~