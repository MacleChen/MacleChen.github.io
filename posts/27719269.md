今日结果：
      1  关联多张表的应用查询：select a.id, a.name , b.classid, b.classname , c.schoolid , c.schoolname from student  a, class b, 
          school c  where  c.schoolid = b.schoolid and b.classid =  a.classid order by id  , name desc;
          关联多张表是2张表至少需要1个条件的限制，3张表至少需要3个条件的限制，否则会出现重复数据。
      2  SQLite3 API   借口函数的应用：
               int sqlite3\_open(const char \* , sqlite3 \*\*db)  // 第一个参数数据库文件地址名， 数据库指针（打开数据库）
               int sqlite3\_close(char \*db) // 数据库指针（关闭数据库）
       3        vc++ 6.0 连接sqlite3数据库方法：必备文件：sqlite3.dll   sqlite3.h    sqlite3.lib
第三步 tool/option/ 选Directories  Show directories for分别加入.h 和.lib所在的目录
第二步 Project/ settings link .
Category 选General,在Object/library modules 加入sqlite3.lib
       4  实例：sqliete3 \*db;  
                     ret =  sqlite\_open("./mydb.dat" , &db)  //   返回0表示成功，其他表示失败
                      sqlite3\_close(db);  // 关闭数据库
                    返回错误信息函数：    char \*msg;   //  原型  const char \* sqlite3\_errmsg(char \*db);
                                                         msg = sqlite3\_errmsg(db);  // 返回最近一次操作数据库API 数据的错误描述
               函数原型：int sqlite3\_prepare(sqlite3 \*, const char \* ,int , sqlite3\_stmt\*\*, const char \*\*)//  预编译SQL（查询）语句
                        实例：sqlite3\_prepare(db, "select  \* from student" , 60  , &pstmt, NULL)
                                                                                                    最大字符数  结果记录集   扩展未使用指针
               取某一行记录的函数： int sqlite3\_step( sqlite\_stmt \*pstmt)   // statement 对象的记录
               从行数据中取字段记录 ： int sqlite3\_column\_int( sqlite\_stmt \*pstmt,  int col)
                         还可以是其他类型  text  .....................text..........
                        实例： sqlite3\_column\_int(stmt, 0)  |  sqlite3\_column\_text(stmt, 1)//   stmt (statement对象记录）0|1表示列
        5  int sqlite3\_finalize( sqlite\_stmt \* pstmt)// 重置对象资源（将读取数据的指针指向起始位置）
            sqlite3\_exec( db ,  sql , NULL, NULL, NULL) //  数据库指针    数据库的操作语句
            直接查询结果集：int sqlite3\_get\_table(sqlite3 \* ,  const char\* sql, char  \*\*\*presult, int \*nrow, int \* ncolumn, char                  \*\*errmsg)// 参数表示：1 数据库练连接结构， 2 sql语句 ， 3 结果集， 4 行数， 5列数，6 错误信息返回
           sqlite3\_free\_table(    char \*\*presult) // 释放表


明日计划：先完成老师布置的作业，其次，对C++的类进行复习，并且主要去编程中学习，最后做结合sqlite3数据库与C++的一些练习题。
感想：感觉上sqlite 只是一个DOS小程序，但越来越发现，他的“五脏俱全”。也很轻巧的可以完成数据库的操作；最近感觉自己经常性的熬夜，可能就是因为这样，自己晚上的梦很多，感觉精神状态不是很佳，看来自己一定要调整过来，至少在12点半之躺在床上。还有就是把自己做事情的节奏加快一些，少一点犹豫，多一点坚定。