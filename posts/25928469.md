今日结果：                                       输出数值                      输出地址
      1  \*\*p:(指针的指针）               \*(\*p+i)                              \*p+i
      2  \*p[5](指针数组）                  \*p[i]                                  p[i]
      3  (\*p)[5](数组指针）                (\*p)[i]                              p[i]
                括号的优先级高
      4  char \*p ; p= (char\*)malloc(10); 给指针p分配10个单位的空间。
      5  int \*max(int a,int b)｛。。。。｝指针函数，其返回值为整型的指针。   
               int（\*p)(int a,int b){.....}  函数指针 ，其返回值为整型的数值。    
      6  结构体自定义变量时，不能声明自身变量，但可以声明自身指针。例如：
                struct  stu  {
                    int id;    int name ;      struct  stu  a;//(错误）   struct  stu   \*p;//正确。
      7  函数也可以当作参数传递。但类型必须匹配或强转。
      8  返回值为整型指针可以正常返回，如果是字符串指针必须加static ,或为其分配内存空间。
                    static  char \*P={"abcdefg"};      char \*p;  p=(char\*)malloc(10);  free(p);  p=NULL;(保险）使用malloc（）函数分配空间后，必须用  free（）函数释放空间。
      9  对于void \*p; p指针可以指向任意类型， printf("%d\n",\*((int\*)p)); (整型强转）  void 类型输出特定类型是必须强转。
                                                                         printf("%s\n",(char\*)p); （字符串强转）
                                                                         printf("%c\n",\*((char\*)p));( 字符强转）
      10  int  \*p;    对于指针p  的sizeof(p)一直都是4。 
   
明日计划：首先完成作业，其次复习，做算法编程题，并且回顾那些理解不充分的知识点，在留一点时间了解具体项目的大体规划和进行链表的预习。
感想：对于培训了半个月时间，自我觉得还是很有收获的，C语言也没有人想象的那么难，只要肯花时间，肯定会有收获的。还是觉得上次回家的时间太短了，只待了5点，就急急忙忙的坐火车来学校了。好想回家。。。和爷爷一起去看陕西秦腔戏，吃我妈妈亲手做的面条，和我爸一起玩开车，和我弟在一起的吵吵闹闹。还有见见家里可爱的小猫小狗。。。希望一切都好。