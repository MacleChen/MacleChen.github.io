```
<!--在出错的activity中，对应布局文件中加入-->
```

```
<!--不能缺少，缺少后出现If this view is optional add '@Nullable' (fields) or '@Optional' (methods) ...-->
<include
    android:id="@+id/view_toolbar"
    layout="@layout/layout_tool_bar"/>
```