今日结果：
        1  上一堂课内容的巩固: 在fork（） 下才产生子进程，关闭文件时，父子进程都要关闭文件（即使子进程没有打开文件，但他会继承父进程的方式）。
2  信号是通信方式的一种，（通信方式还有：信号量，线程，socket，管道等）
                   通信过程主要包括：数据传输，共享数据，通知事件，资源共享，进程控制（如Debug)
        3  linux 进程间的通信（IPC）  管道 pipe       无名管道  FIFO
                     信号的处理方式有三种： SIG\_IGN（屏蔽方式），SIG\_DFL（默认方式一般为退出），自定义方式
        4  安装信号：  （kill  -l  )　查询系统所有的信号
　　　　　　    signal(SIGQUIT, SIG\_IGN);      //   屏蔽信号
 signal(SIGQUIT,  sig\_handle);   //  自定义处理方式
                                    void   sig\_handle(int signo) {printf("退出");    }
                        一个进程也可以安装多个信号
                       有些信号没有办法改变行为属性（如 9号，19号进程）    SIGUSR1，SIGUSR2 用户自定义信号
                       signal(  i,  sig\_handle);
                            sleep(100);        //  上面收到信号时，而停止休眠，然后继续向下运行
发射信号函数： int kill (pid\_t pid,  int sig);          //  进程pid，  信号      
                                          sigqueue(atoi(argv[1]), SIGINT, val)  ;  //  为写打开，信号，         发送信号
                                       返回： 0 成功      -1 失败
                                       不可靠信号：发射信号时有时会丢失， 可靠信号不会丢失信号  
 5  用sigaction 安装信号（重点，难点）（是一个很复杂的函数）
int sigaction(int sig,  const struct sigaction \*act, struct sigaction \*oldact);//   信号，信号活动，旧的信号活动、
                      sigset\_ sa\_mask   // 信号掩码
      sigset\_ sa\_flags   // 信号处理
                  安装信号：    struct  sgaction newact;             设置信号活动的结构体
newact.sa\_handler = SIG\_IGN;           // 设置信号处理方式
main(){  sigaction(SIGINT, &newact,  NULL) ;        for(;;)  sleep(100);}   // 创建信号
int Sigemptyset( sigset\_t \*set);   // 信号清空
...   Sigfillset(...)   ;    // 信号填充
...   Sigdelset(...);    //   信号删除
...  Sigaddset(...);   //   增加信号
  6  阻塞信号：   一种在安装信号之前阻塞信号，另一种是安装信号之后阻塞信号
                      sigprocmask (...);  //   在原有的信号上阻塞，   并且可以检查和改变信号的掩码
                      设置附加消息：    newact.sa\_flags  = SA\_GSIGINFO;
                                                             
//  有附加消息时，要用此选项，其他情况可用默认
           7  管道：  古老的进程间通信方式。（‘  |  ‘）   (ps -ef  | grep   action)
以半双工的方式（数据单项流通）
  pipe   函数：两个进程间的通信不需要调用shell 解释命令
file\_descriptor[0]  :    为读而打开
file\_descriptor[1]  :  为写而打开
   8  无名管道 FIFO：  （必须在家族进程中）
FIFE \* popen(...);   //  允许一个程序去启动另一个程序
fscanf(fp ,  "%d", &cnt) ;    // 从文件中读取内容到cnt中
                                           
明日计划：完成老师布置的信号作业，复习进程，信号等的具体是用。然后去学习一些名词的概念。


感想：自己现在好像忘性增加了，最近经常忘记那东西， 前几天忘记拿眼镜了，昨天忘记拿包裹，晚上上课回来时，又忘记拿衣服了，整个人是越来越浑了。主要解决的方法就是学会在哪儿那东西放哪儿，在放东西的位置处，放置明显的地方，走时检查东西时候齐全，最后学会整理东西并归类。然后培训这边真的来说已经到了重点，难点的地方了，进程，信号，系统函数的灵活使用，自己要花更多的时间去整理，去不断的练习，主要按照自己的想法去对比和使用系统函数去实现。