今日结果：
1.serial port 2： 对应于物理机的ttyS1
针对于文件系统
2.连接串口时：注意串口的配置， 注意串口的使用，禁止多个主机同时使用
一个端口。
3. strstr函数：查找父字符串中是否含有子字符串。
4. 学习用代码进行AT指令读取和写入。然后上午进行AT指令测试，下午进行
尝试将qtopia的代码移植到arm板上运行。
明日计划：再次尝试将可执行程序一致到arm板上，编写更规范的程序在arm板上
运行。
感想：真的不知道硬件都有那些脾气，上午安装好的驱动，下午就重新连接一次
就提示驱动没有安装成功。但是偶尔出去了一下驱动又提示安装成功。在用
source Insight 进行qtopia的代码编写时，1》很大的一个问题就是编写头文件
时，它不会自动提示。2》在调用某个对象的方法时，有的会有提示，有的没有
提示，但是对象的方法确实存在。而且在编译是也会出现一些位置的错误。希望
在接下来的时间里，能够认真的去看开发板中自带的视频教程。来解决当前的这些
问题。