今日结果：
1. linux下的状态分为 用户态， 内核态。 
2. 常用到的函数：get\_user, put\_user,  copy\_from\_user,  copy\_to\_user
3. module\_init(\*\*\*);   // 模块加载时调用
    module\_exit(\*\*\*);  // 模块退出时调用
文件操作结构体中， 成员变量，使用 点语法，（ .open    .write      )
4. 查看设备号： cat/proc/devices    // 主设备号的范围1-255
mknod 'dev/led\_test'     c      232         0                   // 创建设备
表示字符设备      主设备号       次设备号
5. 使用sourceInsight 搭建linux内核编程环境 使用 linux2.6.33
出现一下错误信息：
[root@localhost linux-2.6.33]# make s3c2410\_defconfig
\*\*\*
\*\*\* Can't find default configuration "arch/x86/configs/s3c2410\_defconfig"!
\*\*\*
修改：在linux2.6.33 中的Makefile文件中的180，190行改为 
ARCH ?= arm 
CROSS\_COMPILE ?= arm-linux-
明日计划：最后一次课了，好好珍惜。。。认真听讲
感想：培训已经快要结束了，但是给自己的计划是，首先，和班上的同志们疯狂的玩一把。释放心中的什么什么的哈； 其次，准备为找工作所需的知识进行全面的复习；最后，备考四级这次真的要拼出去了，因为这是自己最后的一搏。