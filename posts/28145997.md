今日结果：（设备（I/O））与文件
        1  linux 中的文件，设备，及高级应用（企业最常用）
2 一些设备文件对应一些文件（前提设备必须要驱动程序）；   应用程序 ---设备文件----驱动模块---硬件
               设备分类： 系统设备  （CPU，内存等）   用户设备（手写板，摄像头等）
按设备信息交换单位来分： 字符设备， 块设备
按设备共享属性来分：独享设备（进程等），共享设备（硬盘等）
3  ulimit -n  查看进程打开多少个文件
系统总共可以打开1024个文件，系统初始化打开，标准输入，标准输出，标准检错。剩下可以打开1021个文件
int open(const char \*path, int flags,  mode\_t mode);    // 文件打开函数，（使用前要给打开文件里写头文件， 失败返回 -1 ）                    文件路径          打开方式       用户访问权限
O\_CREAT    // 文件不存在是建立文件
O\_TRUNC   // 打开文件时，将内容清空
        用man 可以查看函数需要加的头文件          man   open     //  每一个函数都有很可能有重载函数 ， man  2 open 查找第二页的重载函数
创建又读写，又差创建的文件        fd = open("./abc.txt" ,  O\_RDWR  |  O\_CREAT, 0666);
int write(int fd , void \*buf , size\_t nbytes);  // 写入文件的函数          
ret = write(fd , "abcdef", 6);
读文件：  int read(int fd , void \*buf,  size\_t nbytes);  // 读文件的函数       
打开文件返回值        存储读的数据       要读出数据的大小
关闭文件：   close(fd);       // 打开文件的返回值
off\_t lseek( fd , 2 , SEEK\_SET);              //   查找内容，   ...    偏移量            光标位置
4  不可见字符可用其它进制帮助显示出来， （在文件的末尾会有个换行符）
chmod("./abc.txt", 0777);   // 修改文件权限
int  fchmod(int fd ,  mode\_t mode)  ;  // 文件返回值方式修改权限
ret  = chown("./abc.txt", 0,0) ;  //  修改文件所属的所有者和用户组
ret = mkdir("./bak");  //创建目录
  ret = rmdir("./bak");   // 删除空目录
DIR \*　opendir("/root/wf1307");      // 打开目录
struct dirent \* dirinfo;
                         dirinfo =  readdir("/root/wf1307");     // 读出该目录下的所有文件和目录
closedir(dir);    // 关闭目录  ( dir 打开目录时的返回值）
5  文件记录锁：   多个进程同时操作一个文件
int fcntl (int fd , int cmd , struct flock\* lock);   // cmd 功能符号           lock 设置锁
struct flock   lk;  // 锁的结构体
lk.l\_type  = F\_WRLCK;  // 设置锁的类型
lk.l\_whence = SEEK\_SET;    设置起始位置
lk.l\_start = 0;
lk.l\_len = 0;
ret = fcntl(fd , F\_SETLK, &lk);   设置锁
  
明日计划：详细的练习今天所学的内容，认真听明天所学的课，做好笔记，课后练习课堂所学的知识。


感想：本来今天，还有一个兴奋的念头，就是以后和上周一样，周六是下午上课。这样就没有课程冲突了。看来是自己多想了。不过不管怎样自己事情是怎样安排的，培训这边自己应该是放在第一位的。自己现在最要紧做的就是把握住当下应该做什么，然后金全力的做好。加油！