今日结果：
      1  QTablewidget \*txtMemo = new QTableWidget（0，3）;  //  定义显示数据库表对象  （0行，3列）
      2  显示要插入的字段名： Qstringlist ListHead;    //   QTableWidget   \*tab;
                                              tab->setHorizontalHeaderLabels(ListHead);    
                                              ListHead<<"id"<<"name"<<"classid";  
      3  插入项数据： QTableWidgetItem \*item = new QTableWidgetItem("aaa"); 
                                 tab->insertRow(0) ;   // 插入一个空行
                                 tab->setItem(0,1,item)// 将Item数据放在0行1列中,  行列的下标都从零开始
                 setRowcount(int)  ;//  设置行数      edtname->setFocus();  //  移动光标到次输入框
      4  this->accept(); //  接受    this->rejected();//拒绝  （用法还不是很清楚？？）
          exec();  // 内部是无限循环函数，它是QEvent顶级事件；sponlanous 事件由系统产生，通过事件逐个处理
                                                                                                  post 事件而是由程序自己产生。
      5  事件函数：  bool  MYLINEEDIT ::  event (QEvent \*evn)
                             {
                                     if(evn->tpye() == (QEvent::keyPress)
                                     {  QKeyEvent \*keyevn = (QKeyEvent \*) evn;    
                                           if( Keyevn->key == Qt::key\_A)
                                                          this->insert("b");  / /  改写键盘
                                           else 
                                                   return   QWidget::event(evn); //   使用父类事件
                                    }
                                  return  QWidget::event(evn); //   使用父类事件
                               }
              focusNextPrevchild(true);  // 移动光标  true向下移 ， false向上移
       6  事件过滤器：  一个QOBJECT实例可以监听另一个QOBJECT实例中的事件。
                   bool test::eventFilter(Qobject \*obj, QEvent \*evn)
                     {      if(evn->type() == QEvent::keyPress)
                               {   QkeyEvent \*keyevent = (QkeyEvent\*)evn;}  ...//设置按键事件
                      }
               edtId->installEventFilter(this);  // 设置过滤器  
       7  树结构（QtreeWidget)
                 QtreeWidget \*treeWidget = new QtreeWidget;  //  设置树的第一个节点（一级节点）
                treeWidget->setHeaderLabels(QStringList()<<"key"<<"values");  // 设置树的字段
               QTreeWidgetItem \*A = new QtreeWidgetItem(QStringList()<<"86"<<"中国"); //  设置一节结点的字段值
              A->setcheckstate(0, Qt::checked);   // 设置复选框是选中
              treeWidget->expanAll(); //  显示时展开子节点


明日计划：消化老师今天所讲的数据库处理，键盘事件改写，过滤器，树状的复选框等内容，然后完成并且完善登录窗口操作，对另一个界面进行合理的数据处理。最后熟悉Qt接口，方法等。
感想：自己现在感觉到，做项目之间有很多联系的地方，比如说：好的一点是开始对一个项目总体规划是一样，然后，不好的就是都会遇到函数复用性差，对其他的接口，方法不够了解，本应该可以有捷径函数，但自己不知道，然后写了好几个函数，兜了一大圈，才达到目的。所以在做项目时，学会学习别人好的一些方法，一边做，一般去学习陌生的知识点。当然，有好的地基，才能建立稳固的大楼。加油自己~~~